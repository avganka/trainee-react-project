export enum AppRoutes {
  Main = '/',
  Login = '/login',
  Room = '/offer/:id',
  Favorites = '/favorites'
}

export enum AuthStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
